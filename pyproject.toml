[project]
name = "deltify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Esakki", email = "esakkisankart@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "delta-spark==3.2.1",
    "pyyaml>=6.0.2"
]

[dependency-groups]
dev= [
    "pytest>=8.3.4",
    "pytest-cov>=4.1.0",
    "setuptools>=61.0.0",
    "wheel"
]


[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"